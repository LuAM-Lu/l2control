{
  "name": "@l2/domain-identity",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Políticas de acceso: PIN, bloqueo por intentos y estado del dispositivo (ADR-013).",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types src/*.test.ts"
  },
  "comment": "SIN DEPENDENCIAS. Aquí solo viven las reglas; el hash del PIN y la sesión los maneja Better Auth en el servidor, nunca este paquete.",
  "devDependencies": {
    "typescript": "7.0.2"
  }
}
